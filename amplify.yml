version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm --prefix webapp ci
    build:
      commands:
        - npm --prefix webapp run build
  artifacts:
    baseDirectory: webapp/dist
    files:
      - "**/*"
  cache:
    paths:
      - webapp/node_modules/**/*
