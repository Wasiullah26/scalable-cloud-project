# Dependencies bundled in the Lambda zip (no uvicorn – not used in Lambda).
# Used by deploy.py with --platform manylinux2014_x86_64 for Lambda runtime.
fastapi==0.109.2
httpx==0.26.0
pydantic==2.6.1
pydantic-settings==2.1.0
mangum>=0.17.0
PyJWT>=2.8.0
bcrypt>=4.0.1
